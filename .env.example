TAVILY_API_KEY=tvly-your-key-here
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Optional — debug visibility for the ReAct pipeline (default: INFO, off).
# PULSE_LOG_LEVEL=DEBUG    # stdlib logging: LLM calls, Tavily queries, reason
                           # context, observe score breakdown, node enter/exit.
# PULSE_VERBOSE=1          # live per-step progress to stderr as the run
                           # happens, independent of PULSE_LOG_LEVEL.